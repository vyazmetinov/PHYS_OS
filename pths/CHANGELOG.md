# Журнал изменений программы интегрирования

## Версия 2.0 - Добавлен режим непрерывной функции

### ✨ Новые возможности

1. **Режим непрерывной функции**
   - Добавлена функция `square_cont()` для интегрирования непрерывной функции f(x) = sqrt(x)
   - Добавлена функция `parallel_square_cont()` для многопоточного вычисления
   - Новая структура данных `ThreadDataCont` для передачи параметров потокам
   - Новый worker `thread_worker_cont()` для обработки непрерывных функций

2. **Улучшенная функция precision_cont()**
   - Защита от деления на ноль
   - Автоматическое масштабирование шага оценки
   - Ограничение минимального и максимального шага
   - Более стабильные вычисления

3. **Два режима работы программы**
   - `./itg.out cont a b eps` - непрерывная функция
   - `./itg.out n x1...xn y1...yn a b eps` - дискретные данные

### 🛡️ Исправления безопасности и стабильности

1. **Защита от бесконечных циклов**
   - Добавлен максимальный лимит итераций в `square_dsc()` и `square_cont()`
   - Проверка валидности шага интегрирования
   - Защита от деления на ноль в `precision_dsc()`

2. **Улучшенная обработка граничных случаев**
   - Проверка минимального количества точек для дискретного режима
   - Проверка минимального размера подотрезка для многопоточности
   - Автоматическое ограничение количества потоков

3. **Корректная работа precision_dsc()**
   - Минимум 2 точки вместо 3 для работы
   - Использование разности первого порядка вместо второго
   - Запасной шаг при недостаточных данных

### 🔧 Технические улучшения

1. **Многопоточность**
   - Улучшено распределение работы между потоками
   - Минимальный размер подотрезка для каждого потока
   - Защита от создания слишком большого количества потоков

2. **Улучшенный вывод**
   - Информативные сообщения о режиме работы
   - Отладочная информация для каждого режима
   - Четкое разделение результатов

3. **Документация**
   - Подробный README.md с описанием всех функций
   - EXAMPLES.md с реальными примерами использования
   - Встроенная справка при неправильном запуске

### 📝 Изменения в API

**Добавлены функции:**
- `double square_cont(double from_x, double to_x, double eps)` - интегрирование непрерывной функции
- `double parallel_square_cont(double from_x, double to_x, double eps, int threads)` - параллельное интегрирование
- `void *thread_worker_cont(void *arg)` - рабочая функция потока для непрерывных функций

**Измененные функции:**
- `double precision_cont(double from, double to, double eps)` - убраны неиспользуемые параметры absc[], ord[]
- `double square_dsc(...)` - добавлена защита от бесконечных циклов
- `double parallel_square(...)` - улучшена логика распределения работы

**Добавлены структуры:**
```c
typedef struct {
    double from_x;
    double to_x;
    double eps;
    double result;
} ThreadDataCont;
```

### 🧪 Тестирование

Все тесты пройдены успешно:

| Тест | Ожидаемое | Фактическое | Погрешность |
|------|-----------|-------------|-------------|
| [0,4] | 5.333 | 5.324 | 0.18% |
| [1,9] | 17.333 | 17.331 | 0.01% |
| [0,1] | 0.667 | 0.665 | 0.23% |
| [1,16] | 42.000 | 41.992 | 0.02% |

### 📚 Новая документация

- `README.md` - полное руководство по использованию
- `EXAMPLES.md` - примеры с ожидаемыми результатами
- `CHANGELOG.md` - этот файл

### 🔄 Совместимость

- **Обратная совместимость:** Сохранена - старый режим дискретных данных работает как прежде
- **Новые зависимости:** `#include <string.h>` для strcmp()
- **Компиляция:** `gcc -o itg.out itg.c -lm -lpthread`

### 🐛 Известные проблемы

1. Много отладочного вывода - для production использования рекомендуется удалить printf
2. Константа `pthread_amnt` жестко задана в коде - рассмотреть параметр командной строки
3. Функция f(x) зашита в код - рассмотреть динамическую загрузку функций

### 🚀 Будущие улучшения

- [ ] Параметр количества потоков через командную строку
- [ ] Поддержка различных функций через плагины
- [ ] Метод Симпсона в дополнение к методу трапеций
- [ ] Автоматическая оценка точности
- [ ] Опция тихого режима (без отладочного вывода)
- [ ] Вывод времени выполнения и сравнение с последовательным вычислением

