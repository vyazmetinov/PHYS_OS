# Подробные пояснения ко всем файлам проекта

## 📋 Оглавление
1. [Корневые файлы](#корневые-файлы)
2. [Директория msg_queue/ - Очереди сообщений](#директория-msg_queue)
3. [Директория pths/ - Потоки](#директория-pths)
4. [Директория semafores/ - Семафоры](#директория-semafores)
5. [Директория files/](#директория-files)

---

## Корневые файлы

### 📄 main.c

**Назначение**: Главный файл с демонстрацией различных механизмов межпроцессного взаимодействия (IPC - Inter-Process Communication).

**Что делает этот файл:**

Файл содержит несколько функций-задач, демонстрирующих разные аспекты работы с операционной системой:

#### 1. **trim_newline()** (строки 17-21)
- Убирает символы новой строки (`\n`, `\r`) из конца текста
- Нужна для корректной обработки строк, прочитанных из файлов

#### 2. **is_dir_path()** (строки 24-28)
- Проверяет, является ли путь директорией или файлом
- Использует системный вызов `stat()` для получения информации о файле

#### 3. **first_task()** (строки 33-50)
- Читает файл `comm.txt` с командами
- Каждая строка содержит: задержку (в секундах) и команду для выполнения
- Выполняет команды через `system()` с заданными интервалами

**Пример**: Если в файле написано `2 ls`, программа подождёт 2 секунды и выполнит команду `ls`.

#### 4. **copy_files()** (строки 63-139)
- Рекурсивно копирует все файлы и директории из одной папки в другую
- После копирования **сжимает** все файлы с помощью gzip
- Работает в два прохода:
  - Первый проход: копирует и сжимает файлы
  - Второй проход: создаёт подпапки и рекурсивно обрабатывает их

**Для чего**: Создание резервных копий с автоматическим сжатием.

#### 5. **second_task()** (строки 140-149)
- Обёртка для `copy_files()`
- Читает пути из стандартного ввода

#### 6. **PI()** (строки 151-230) - Работа с разделяемой памятью
Это демонстрация работы с **shared memory** (разделяемая память):

**Что происходит:**
1. Создаётся ключ с помощью `ftok()` - это уникальный идентификатор для IPC-объектов
2. Определяется размер файла `main.c`
3. Создаётся сегмент разделяемой памяти через `shmget()`
4. Память подключается к процессу через `shmat()`
5. Содержимое файла `main.c` копируется в разделяемую память
6. Память отключается через `shmdt()`
7. Затем снова подключается и читается оттуда
8. В конце память удаляется через `shmctl()`

**Зачем это нужно**: Разделяемая память позволяет разным процессам читать и писать в одну и ту же область памяти, что быстрее, чем другие способы обмена данными.

#### 7. **fourth_task()** (строки 232-276)
- Демонстрирует работу с разделяемой памятью между запусками программы
- Создаёт сегмент памяти с одним числом
- Увеличивает это число на 1
- При повторном запуске программа увидит увеличенное значение

**Важно**: В строке 274 есть комментарий про `shmctl()` - его нужно раскомментировать после демонстрации, чтобы очистить память.

#### 8. **fifth_task()** (строки 278-335)
- Демонстрирует работу с разделяемой памятью между **родительским и дочерним процессами**
- Использует `fork()` для создания дочернего процесса
- Дочерний процесс увеличивает значение в памяти
- Родительский процесс читает это значение

**Разница с fourth_task**: Здесь два процесса работают одновременно, а не последовательно.

#### 9. **main()** (строки 341-365)
- Демонстрирует работу с семафорами System V
- Создаёт семафор и выполняет операцию P (уменьшение счётчика)
- Выводит "POTREBITEL" (потребитель)

**Что такое семафор**: Это счётчик, который помогает процессам синхронизироваться. Например, ограничить количество процессов, работающих одновременно.

---

### 📄 main.cpp

**Назначение**: Простой пример на C++ (не связан с остальным проектом).

**Что делает:**
- Читает число `a`
- Выводит это число `a` раз

**Проблема в коде**: В цикле `for (int i; i< a ; i++)` переменная `i` не инициализирована, что может привести к непредсказуемому поведению.

---

### 📄 second_task.c

**Назначение**: Упрощённая версия функции копирования из `main.c`.

**Что делает:**
- Копирует файлы и директории из одной папки в другую
- Сжимает скопированные файлы через gzip
- Работает рекурсивно (обрабатывает вложенные папки)

**Использование**: Запускается с двумя аргументами командной строки:
```bash
./second_task /путь/откуда /путь/куда
```

---

## Директория msg_queue/

Эта директория содержит примеры работы с **очередями сообщений** (message queues) - механизмом IPC для обмена данными между процессами.

### 📄 cs.c (Client-Server)

**Назначение**: Демонстрация модели клиент-сервер с использованием очередей сообщений.

**Архитектура:**
- Один сервер постоянно слушает очередь
- Несколько клиентов отправляют сообщения

**Как работает:**

#### Клиент (функция `client`, строки 15-33):
1. Создаёт сообщение "Hello from client [PID]"
2. Отправляет 10 сообщений в очередь с интервалом 1 секунда
3. Использует `msgsnd()` для отправки

#### Сервер (функция `server`, строки 35-51):
1. Постоянно ждёт сообщений в очереди
2. Получает сообщения через `msgrcv()`
3. Выводит полученные сообщения

**Запуск:**
```bash
# В одном терминале (сервер):
./cs s

# В других терминалах (клиенты):
./cs c
```

**Что такое очередь сообщений**: Это список сообщений, где процессы могут оставлять данные друг для друга. Как почтовый ящик: один кладёт письмо, другой достаёт.

---

### 📄 itg.c (Integration)

**Назначение**: Вычисление определённого интеграла функции sin(x) методом трапеций с использованием потоков и очередей сообщений.

**Математика**: Интеграл ∫sin(x)dx приближается суммой маленьких трапеций.

**Как работает:**

1. **Входные данные**: Границы интегрирования `a` и `b`
2. **Разбиение**: Отрезок [a,b] делится на 100,000 частей
3. **Многопоточность**: Для каждой части создаётся поток
4. **Коммуникация через очереди**:
   - Каждый поток вычисляет площадь своей трапеции
   - Результат отправляется в очередь сообщений
   - Главный поток получает результаты и суммирует их

**Функция `square()`** (строки 25-41):
- Вычисляет площадь трапеции: `(f(x1) + f(x2)) / 2 * (x2 - x1)`
- Отправляет результат в очередь

**Использование:**
```bash
./itg 0 3  # Интеграл от 0 до 3
```

**Зачем очереди здесь**: Это способ безопасно передать результат от потока обратно в главную программу.

---

### 📄 sem.c

**Назначение**: Реализация **собственной системы семафоров** на основе очередей сообщений и разделяемой памяти.

**Почему "собственная"**: Вместо использования готовых семафоров System V или POSIX, автор создал свою реализацию для учебных целей.

**Архитектура:**

#### Структуры данных:
1. **`my_sem`** (строки 17-22): Пользовательский семафор
   - `sem_num`: идентификатор
   - `sem_op`: текущее значение счётчика
   - `sem_msg_id`: ID очереди сообщений для этого семафора

2. **`sem_buf`** (строки 24-27): Таблица всех семафоров
   - Массив на 128 семафоров
   - Счётчик использованных семафоров
   - Хранится в разделяемой памяти (доступна всем процессам)

#### Основные функции:

**`create_buf()`** (строки 31-65):
- Создаёт или подключается к разделяемой памяти для таблицы семафоров
- Инициализирует mutex-семафор (индекс 0) для защиты самой таблицы

**`create_sem(int init_val)`** (строки 68-91):
- Создаёт новый семафор с начальным значением
- Возвращает его ID
- Создаёт очередь сообщений для этого семафора

**`lock_sem_tbl()` и `unlock_sem_tbl()`** (строки 93-121):
- Блокируют/разблокируют доступ к таблице семафоров
- Используют семафор с индексом 0 как mutex

**`sem_wait(my_sem *sem, int P)`** (строки 123-146):
- Реализация операции P (уменьшение семафора)
- Если счётчик >= P, уменьшает его и возвращается
- Если счётчик < P, ждёт сообщения в очереди

**`sem_post(my_sem *sem, int op)`** (строки 148-168):
- Реализация операции V (увеличение семафора)
- Увеличивает счётчик
- Отправляет сообщение в очередь, чтобы разбудить ожидающие процессы

**`destroy_sem()` и `destroy_all_sems()`** (строки 171-253):
- Удаляют семафоры и освобождают ресурсы

**Применение**: Этот код - библиотека, которую можно использовать в других программах для синхронизации процессов.

---

### 📄 t_w_c.c (Two Way Communication)

**Назначение**: Демонстрация двустороннего общения через очереди сообщений.

**Участники:**
1. **hellower**: Отправляет "hello world"
2. **byier**: Отправляет "bye world"

**Протокол общения:**

```
Цикл 10 раз:
1. hellower отправляет "hello world"
2. byier получает "hello world"
3. byier отправляет "bye world"
4. hellower получает "bye world"
```

**Функции:**

**`hellower()`** (строки 17-46):
- Если `iter > 0`: сначала получает сообщение
- Затем отправляет "hello world"
- Ждёт 2 секунды

**`byier()`** (строки 48-75):
- Получает сообщение
- Отправляет "bye world"
- Ждёт 2 секунды

**Запуск:**
```bash
# В одном терминале:
./t_w_c hi

# В другом терминале:
./t_w_c bye
```

**Наблюдение**: Процессы синхронизируются через очередь - один ждёт, пока другой отправит сообщение.

---

## Директория pths/

Эта директория содержит программы для работы с **потоками** (threads) - лёгкими процессами внутри одной программы.

### 📄 integral.c

**Назначение**: Вычисление интеграла функции sin(x) методом трапеций с использованием многопоточности.

**Отличие от msg_queue/itg.c**: Здесь для передачи результатов используется **mutex** и общая переменная, а не очереди сообщений.

**Как работает:**

1. **Общие данные:**
   - `pthread_mutex_t mutex`: блокировка для защиты общей переменной
   - `double res`: накопитель результата (общая переменная)

2. **Функция `square()`** (строки 20-28):
   - Вычисляет площадь трапеции
   - **Блокирует mutex** перед изменением `res`
   - Добавляет результат к общей сумме
   - **Разблокирует mutex**

3. **main()** (строки 30-51):
   - Создаёт 100,000 потоков
   - Каждый поток обрабатывает один маленький отрезок
   - Ждёт завершения каждого потока (`pthread_join`)
   - Выводит финальный результат

**Что такое mutex**: Это "замок" для данных. Только один поток может держать замок открытым. Это предотвращает конфликты при изменении общей переменной.

**Использование:**
```bash
./integral 0 3  # Интеграл от 0 до 3
```

**Почему так много потоков**: Для демонстрации. В реальности создавать 100,000 потоков неэффективно - лучше использовать пул потоков.

---

## Директория semafores/

Эта директория содержит классические задачи синхронизации, решаемые с помощью **семафоров**.

### 📄 birds.c

**Назначение**: Решение задачи "Мать-птица и птенцы".

**Условие задачи:**
- Есть мать-птица и N птенцов
- В кормушке помещается F порций еды
- Когда кормушка пустеет, мать наполняет её
- Птенцы едят по очереди

**Реализация:**

**Семафоры:**
1. `sem_mom`: Семафор для матери (начальное значение = 1)
2. `sem_child`: Семафор для птенцов (начальное значение = F)

**Функция `mother()`** (строки 21-29):
- Ждёт вызова (через `sem_wait(&sem_mom)`)
- Наполняет кормушку: `srv = F`
- Разрешает птенцам есть: `sem_post(&sem_child)`

**Функция `child()`** (строки 31-39):
- Ждёт доступа к еде: `sem_wait(&sem_child)`
- Ест (уменьшает `srv`)
- Если `srv < 1`, вызывает мать

**main()** (строки 41-61):
- Создаёт N птенцов
- Когда кормушка пуста (`srv < 1`), запускает поток матери

**Запуск:**
```bash
./birds 10 3  # 10 птенцов, 3 порции в кормушке
```

**Вывод:**
```
1-й птенец приступает к еде
1-й птенец поел
2-й птенец приступает к еде
2-й птенец поел
...
Кушайте гости дорогие на базаре все так дорого
...
```

---

### 📄 phylocofy.c (Philosophers)

**Назначение**: Решение классической задачи "Обедающие философы".

**Условие задачи:**
- 5 философов сидят за круглым столом
- Между каждыми двумя философами лежит одна вилка (всего 5 вилок)
- Чтобы есть, философу нужны ДВЕ вилки (слева и справа)
- Задача: организовать приём пищи без взаимной блокировки (deadlock)

**Решение - алгоритм "Дворецкого":**

Добавляется семафор `butler` со значением 4. Это означает, что максимум 4 философа могут одновременно пытаться взять вилки. Это гарантирует, что хотя бы один философ сможет взять обе вилки.

**Реализация:**

**Семафоры:**
1. `forks[5]`: Каждая вилка - отдельный семафор (значение = 1)
2. `butler`: Дворецкий (значение = 4)

**Функция `philosopher()`** (строки 32-54):
```
Бесконечный цикл:
1. Думает
2. Просит разрешения у дворецкого: sem_wait(butler)
3. Берёт левую вилку: sem_wait(forks[left])
4. Берёт правую вилку: sem_wait(forks[right])
5. Ест
6. Кладёт обе вилки: sem_post(forks[left/right])
7. Освобождает дворецкого: sem_post(butler)
```

**main()** (строки 56-91):
- Создаёт именованные семафоры POSIX через `sem_open()`
- Запускает 5 потоков-философов
- Ждёт их завершения (которое никогда не произойдёт - бесконечный цикл)
- Очищает семафоры

**Почему используются именованные семафоры**: Потому что `sem_open()` создаёт семафоры, доступные между процессами. Хотя здесь используются потоки, это демонстрация техники.

**Вывод:**
```
Philosopher 0 is thinking
Philosopher 0 took left fork
Philosopher 0 took right fork
Philosopher 0 is eating
Philosopher 0 put down both forks
Philosopher 1 is thinking
...
```

---

### 📄 producer-consumer.c

**Назначение**: Решение классической задачи "Производитель-потребитель" с использованием кольцевого буфера.

**Условие задачи:**
- Производитель генерирует данные
- Потребитель обрабатывает данные
- Между ними - буфер ограниченного размера (8 элементов)
- Производитель не должен переполнять буфер
- Потребитель не должен читать из пустого буфера

**Реализация:**

**Структура данных `shm_ring_t`** (строки 27-31):
- Кольцевой буфер на 8 элементов
- `head`: индекс для чтения
- `tail`: индекс для записи

**Семафоры:**
1. `SEM_EMPTY`: Количество свободных мест (начально = 8)
2. `SEM_FULL`: Количество занятых мест (начально = 0)
3. `SEM_MUTEX`: Защита доступа к буферу (начально = 1)

**Функции P() и V()** (строки 33-40):
- `P(semid, num)`: Уменьшить семафор (операция "захват")
- `V(semid, num)`: Увеличить семафор (операция "освобождение")

**Функция `produce()`** (строки 42-49):
```
1. P(SEM_EMPTY) - жди свободного места
2. P(SEM_MUTEX) - захвати буфер
3. Положи элемент в буфер
4. V(SEM_MUTEX) - освободи буфер
5. V(SEM_FULL) - сообщи о новом элементе
```

**Функция `consume()`** (строки 51-60):
```
1. P(SEM_FULL) - жди элемента
2. P(SEM_MUTEX) - захвати буфер
3. Возьми элемент из буфера
4. V(SEM_MUTEX) - освободи буфер
5. V(SEM_EMPTY) - сообщи о свободном месте
```

**main()** (строки 62-121):
- Создаёт разделяемую память для буфера
- Создаёт набор семафоров
- Инициализирует семафоры при первом запуске
- В зависимости от аргумента работает как производитель или потребитель

**Запуск:**
```bash
# В одном терминале:
./producer-consumer producer

# В другом терминале:
./producer-consumer consumer
```

**Что происходит:**
- Производитель генерирует числа 0, 1, 2, ..., 99
- Потребитель получает и выводит эти числа
- Если буфер полон, производитель ждёт
- Если буфер пуст, потребитель ждёт

---

## Директория files/

### 📄 itg.c

**Назначение**: Вычисление интеграла с сохранением промежуточных результатов в файл.

**Отличие от других версий**: Результаты накапливаются не в переменной в памяти, а в файле `sq.txt`.

**Как работает:**

**Функция `square()`** (строки 22-41):
1. Блокирует mutex
2. Создаёт жёсткую ссылку на файл `sq.txt`
3. Открывает файл
4. Читает текущее значение интеграла
5. Добавляет площадь своей трапеции
6. Записывает обновлённое значение обратно в файл
7. Закрывает файл
8. Разблокирует mutex

**main()** (строки 43-64):
- Создаёт файл `sq.txt`
- Создаёт потоки для вычисления
- Каждый поток добавляет свой результат в файл

**Зачем использовать файл**: Это демонстрация работы с файлами из многопоточной программы. В реальности использовать память эффективнее.

**Использование:**
```bash
./itg 0 3
```

---

## Общие концепции

### 🔧 IPC (Inter-Process Communication)

Все эти программы демонстрируют различные механизмы IPC:

1. **Разделяемая память (Shared Memory)**
   - Самый быстрый способ
   - Процессы читают/пишут в одну область памяти
   - Файлы: `main.c` (функции PI, fourth_task, fifth_task)

2. **Очереди сообщений (Message Queues)**
   - Процессы отправляют друг другу структурированные сообщения
   - Безопаснее, чем разделяемая память
   - Файлы: всё в `msg_queue/`

3. **Семафоры**
   - Счётчики для синхронизации
   - Используются для предотвращения гонок и тупиков
   - Файлы: `main.c` (функция main), `msg_queue/sem.c`, всё в `semafores/`

4. **Мьютексы (Mutexes)**
   - Специальный вид семафора для взаимного исключения
   - Файлы: всё в `pths/`, `files/itg.c`

### 🧵 Многопоточность (Multithreading)

Программы используют потоки POSIX (pthread):

- **Создание**: `pthread_create(&thread, NULL, function, &args)`
- **Ожидание**: `pthread_join(thread, NULL)`
- **Синхронизация**: мьютексы и семафоры

### 📊 Практическое применение

Эти примеры полезны для понимания:

1. **Параллельного программирования** - как разделить задачу между потоками
2. **Синхронизации** - как избежать конфликтов при доступе к общим данным
3. **Классических задач CS** - философы, производитель-потребитель
4. **Системного программирования** - работа с IPC механизмами Unix/Linux

### ⚠️ Важные замечания

1. **Очистка ресурсов**: Многие программы не удаляют IPC-объекты. После работы проверьте:
   ```bash
   ipcs -a  # Посмотреть все IPC-объекты
   ipcrm -m [shmid]  # Удалить разделяемую память
   ipcrm -q [msgid]  # Удалить очередь
   ipcrm -s [semid]  # Удалить семафор
   ```

2. **Производительность**: Некоторые примеры создают слишком много потоков (100,000) - это для демонстрации, не для production.

3. **Безопасность**: Использование `system()` с пользовательским вводом опасно.

---

## 🎓 Выводы

Этот проект - учебный набор примеров для изучения:
- Механизмов межпроцессного взаимодействия
- Многопоточного программирования
- Классических задач синхронизации
- Системных вызовов Unix/Linux

Каждая программа решает конкретную задачу и демонстрирует определённую технику. Рекомендуется изучать их последовательно, начиная с простых (t_w_c.c, pths/integral.c) и переходя к более сложным (sem.c, phylocofy.c).

